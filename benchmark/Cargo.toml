[package]
name = "benchmark"
version = "0.1.0"
authors = ["Matthew Brookes <mbrookes1304@gmail.com>"]
edition = "2018"

[dependencies]
faster-rs= { path = "../" }
clap = "2.33.0"
libc = "0.2"
regex = "1"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde-big-array = "0.5"
